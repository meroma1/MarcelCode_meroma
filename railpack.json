{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [
        "npm run build -w packages/shared",
        "npx prisma generate --schema=packages/proxy/src/prisma/schema.prisma",
        "..."
      ]
    }
  }
}
